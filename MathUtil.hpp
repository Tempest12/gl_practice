#ifndef _MATH_UTIL_HPP
#define _MATH_UTIL_HPP

class MathUtil
{

// Variables:
public:
protected:
private:

// Functions:
public:

    static float degreeCos(float angle);
    static float degreeSin(float angle);
    static float degreeTan(float angle);

    static float radianCos(float angle);
    static float radianSin(float angle);
    static float radianTan(float angle);

    static float degreesToRadians(float degrees);
    static float radiansToDegrees(float radians);

protected:
private:
};

#endif